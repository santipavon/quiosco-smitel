name: Compilar Quiosco iOS

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
    - name: Descargar el codigo del repositorio
      uses: actions/checkout@v4.2.2

    - name: Instalar Node.js
      uses: actions/setup-node@v4.2.0
      with:
        node-version: 18 # Usamos Node 18, ideal para versiones estables de Cordova

    - name: Instalar Cordova
      run: npm install -g cordova@11.1.0

    - name: Crear Proyecto Cordova Puro (Fijado para iOS 12)
      run: |
        cordova create app com.smitel.quiosco QuioscoSmitel
        cd app
        
        # Forzamos la plataforma nativa estable que no inyecta Swift ni SceneDelegate
        cordova platform add ios@6.3.0
        
        # Mover tu HTML y Logo de pantalla a la carpeta de la app
        rm www/index.html
        cp ../index.html www/
        cp ../logo.png www/ 2>/dev/null || :
        
        # Copiar el icono de la máquina de fichar a la raíz de la app
        cp ../icon.png . 2>/dev/null || :
        
        # Instalar plugin de la camara estándar
        cordova plugin add cordova-plugin-camera
        
        # Inyectar las directivas limpias de iOS 12 en el config.xml global
        sed -i '' 's|<platform name="ios">|<platform name="ios">\n    <preference name="DeploymentTarget" value="12.0" />\n    <icon src="icon.png" />|g' config.xml
        
        cordova prepare ios

    - name: Compilación Nativa Limpia
      run: |
        cd app/platforms/ios
        
        # Compilamos usando la estructura clásica sobre el Workspace oficial
        xcodebuild -workspace QuioscoSmitel.xcworkspace \
          -scheme QuioscoSmitel \
          -configuration Debug \
          -sdk iphonesimulator \
          -destination "generic/platform=iOS Simulator" \
          IPHONEOS_DEPLOYMENT_TARGET=12.0 \
          ARCHS="arm64" \
          ONLY_ACTIVE_ARCH=NO \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGN_IDENTITY="" \
          clean build

    - name: Construir archivo .IPA oficial para Sideloadly
      run: |
        echo "Localizando el ejecutable real..."
        APP_PATH=$(find ~ -name "QuioscoSmitel.app" -type d 2>/dev/null | grep -v "BuildProductsPath" | head -n 1)
        
        if [ -z "$APP_PATH" ]; then
          echo "Error crítico: No se encontró ningún binario ejecutable."
          exit 1
        fi
        
        mkdir -p ipa_structure/Payload
        cp -R "$APP_PATH" ipa_structure/Payload/
        
        # Forzar los metadatos de despliegue finales en el Info.plist para iOS 12
        plutil -replace MinimumOSVersion -string "12.0" ipa_structure/Payload/QuioscoSmitel.app/Info.plist
        
        cd ipa_structure
        zip -r ../QuioscoSmitel.ipa Payload
        cd ..

    - name: Guardar el archivo ejecutable resultante (.ipa)
      uses: actions/upload-artifact@v4
      with:
        name: App-Quiosco-Smitel-IPA
        path: QuioscoSmitel.ipa
