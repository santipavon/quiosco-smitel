name: Compilar Quiosco iOS

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Descargar el codigo del repositorio
      uses: actions/checkout@v4.2.2

    - name: Instalar Node.js
      uses: actions/setup-node@v4.2.0
      with:
        node-version: 22

    - name: Instalar Cordova
      run: npm install -g cordova

    - name: Crear Proyecto Cordova
      run: |
        cordova create app com.smitel.quiosco QuioscoSmitel
        cd app
        cordova platform add ios
        
        # Mover tu HTML y Logo de pantalla a la carpeta de la app
        rm www/index.html
        cp ../index.html www/
        cp ../logo.png www/ 2>/dev/null || :
        
        # Copiar el icono de la máquina de fichar a la raíz de la app
        cp ../icon.png . 2>/dev/null || :
        
        # Instalar plugin de la camara
        cordova plugin add cordova-plugin-camera
        
        # Configurar de forma limpia el icono y la versión de iOS destino en el bloque nativo correcto
        sed -i '' 's|<platform name="ios">|<platform name="ios">\n    <preference name="DeploymentTarget" value="12.0" />\n    <icon src="icon.png" />|g' config.xml
        
        echo "=== VERIFICACIÓN DE CONFIG.XML ==="
        cat config.xml

    - name: Compilar App Real con Cordova
      run: |
        cd app
        cordova build ios --emulator

    - name: Construir archivo .IPA oficial para Sideloadly
      run: |
        echo "Localizando la App y construyendo la estructura oficial..."
        APP_PATH=$(find ~ -name "*.app" -type d 2>/dev/null | grep -v "BuildProductsPath" | head -n 1)
        
        if [ -z "$APP_PATH" ]; then
          echo "Error crítico: No se encontró el compilado .app"
          exit 1
        fi
        
        echo "App encontrada en: $APP_PATH"
        mkdir -p ipa_structure/Payload
        cp -R "$APP_PATH" ipa_structure/Payload/
        
        cd ipa_structure
        zip -r ../QuioscoSmitel.ipa Payload
        cd ..

    - name: Guardar el archivo ejecutable resultante (.ipa)
      uses: actions/upload-artifact@v4
      with:
        name: App-Quiosco-Smitel-IPA
        path: QuioscoSmitel.ipa
